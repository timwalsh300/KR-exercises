Page 121: Given the sort program (from exercise 5-7 and 5-14) add the
option -d for "directory order" which makes comparisons only on letters,
numbers, and blanks. Make sure it works in conjunction with -f.

compile with... gcc main.c lines.c alloc.c qsort.c flags.h

enter the EOF signal with CTRL-D after entering the lines to be sorted
