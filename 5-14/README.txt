Page 121: Modify the sort program (from exercise 5-7) to handle a -r flag
which indicates sorting in reverse (decreasing) order. Be sure that it also 
works with the -n flag shown in this section.

compile with... gcc main.c lines.c mygetline.c alloc.c qsort.c flags.h

enter the EOF signal with CTRL-D after entering the lines to be sorted
