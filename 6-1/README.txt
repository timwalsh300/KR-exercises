Page 136: Rewrite the getword function so that it properly
handles underscores, string constants, comments, and preprocessor
control lines.

I don't see any problem with underscores, but I implemented
everything else.

compile with gcc main.c getch.c freq.h
