Page 126: Make the dcl program recover from input errors, so
that invalid declarations produce some kind of helpful output.

Input function declarations without the trailing semi-colon
using the 'enter' key, then input CTRL-D when done.

compile with gcc main.c dcl.c getch.c dcl.h
