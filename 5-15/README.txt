Page 121: Given the sort program (from exercise 5-7 and 5-14) add the
option -f to fold upper and lower case together, so that case distinctions
are not made during sorting; for example 'A' and 'a' compare equal.

compile with... gcc main.c lines.c mygetline.c alloc.c qsort.c flags.h

enter the EOF signal with CTRL-D after entering the lines to be sorted
