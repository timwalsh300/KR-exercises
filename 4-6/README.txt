Page 79: Given the same calculator program, add commands for handling
variables. It's easy to provide twenty-six variables with single
letter names. Add a variable for the most recently computed value.

Note: I am breaking the program up into multiple files as shown later in
the book on page 82.

Compile with... gcc calc.h getch.c getop.c main.c stack.c -lm

